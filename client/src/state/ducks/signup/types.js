/* ACTION TYPE CONSTANTS
You can use any convention you wish here,
but the name should remain UPPER_SNAKE_CASE for consistency.
*/

const REQUEST = "fab_loch/signup/REQUEST"
const SUCCESS = "fab_loch/signup/SUCCESS"
const FAILURE = "fab_loch/signup/FAILURE"

export default {
  REQUEST,
  SUCCESS,
  FAILURE,
}
